# 问题清单 · 机娘放置挂机游戏（机甲远征）

- 版本：v0.5（MVP 收尾）
- 维护：总指挥 A
- 说明：F5 试玩 + D 只读验收发现的问题；逐条裁决 → 派单 → 回归。优先级：🔴 影响玩 / 🟡 影响体验 / 🟢 锦上添花。

| # | 问题 | 来源 | 类型 | 优先级 | 裁决 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 金币涨得太快 | 试玩反馈 | 数值调整 | 🔴 | 产出降速（0.5/秒、每关 ×1.25） | ✅ 闭环（F5 回归通过） |
| 2 | 战斗压力太小 | 试玩反馈 | 数值调整 | 🔴 | 敌人上调（L1 攻14/血120、+22%/关）；5v5 结构预留 | ✅ 闭环（F5 回归通过） |
| 3 | 挂机改"点一下收获" | 试玩反馈 | 机制变更 | 🔴 | 按实际时长（离线也算）；契约 v0.4 | ✅ 闭环（含离线计时实测） |
| 4 | 升级改"经验+金币"双消耗、战斗掉经验 | 试玩反馈 | 机制变更 | 🔴 | 只胜利给（非首通重复也给）；契约 v0.4 | ✅ 闭环（F5 回归通过） |
| 5 | （后续）指挥官经验/等级 | 试玩反馈 | 阶段 3 | 🟢 | 归阶段 3 | 挂起（阶段 3 再做） |
| 6 | 返回主界面时战斗未中止 | D 验收 | 规则+小修 | 🔴 | 返回=中止，新增 Game.stop_battle()（契约 v0.5） | ✅ 闭环（实测：返回后无自动结算） |
| 7 | 阵亡机娘是否得经验不明确 | D 验收 | 规则澄清 | 🟡 | 出战即算（含阵亡），契约 v0.5 §1.3 | ✅ 闭环（契约写明） |
| 8 | 升级按钮无禁用态/提示 | D 验收 | 体验小修 | 🟡 | 本轮做；新增 Game 只读费用入口（契约 v0.5） | ✅ 闭环（实测：置灰+提示正常） |
| 9 | 主界面无"上次通关奖励"消息 | D 验收 | 体验小修 | 🟢 | 本轮做（Game 内存态 last_clear） | ✅ 闭环（实测正常） |
| 10 | F5 硬验收 4 项（信号时序/存档读写/刷新进度/离线计时） | D 验收 | 必补测 | 🔴 | 真人 F5 回归 | ✅ 闭环（用户手工 F5 全部通过） |
| 11 | "上次通关"消息与即时消息共用 label 互相覆盖 | D 复核 | 体验小修 | 🟡 | 独立标签展示（C 小补丁，不动契约） | ✅ 闭环（C 已交付，实测正常） |
| 12 | 重复通关显示"+0 金币"文案 | D 复核 | 体验小修 | 🟡 | 非首通不显示奖励金额（C 小补丁） | ✅ 闭环（C 已交付，实测正常） |

> 结论：MVP（阶段 0）体验/规则问题全部闭环；剩余待办仅收尾流程（D 最终签核（可选）→ 备份 → GitHub → 导出网页）。观察项（不阻塞）：UI 首屏快照按 Data 公式换算满血值与 exp_next，费用部分已由 v0.5 只读入口消除。

## v0.8 战斗 2.0 新增问题

| # | 问题 | 来源 | 类型 | 优先级 | 裁决 | 状态 |
|---|---|---|---|---|---|---|
| 13 | game.gd 编译错误 ×6（第 801/881/882/916/935/946 行 `:=` 推断三元表达式失败）→ Game autoload 加载失败、无法启动 | C 验收报告 + A 复核 | BUG | 🔴 | 已定位：`:=` 改显式类型/普通赋值 | ✅ 闭环（B 已修复，F5 无报错） |
| 14 | game.gd:510 敌方技能 `skill.id` 点访问（Data.ENEMY_SKILLS 无 "id" 键）→ 敌方放技能报错 | C 验收报告 + A 复核 | BUG | 🔴 | 已定位：改 `skill.get("id", ...)` 兜底 | ✅ 闭环（B 已修复，F5 无报错） |

> B 修复 → F5 启动验证 → C 已用临时副本验证 UI（修复后正常）→ D 整体验收。

## v0.8 D 验收发现

| # | 问题 | 来源 | 类型 | 优先级 | 裁决 | 状态 |
|---|---|---|---|---|---|---|
| 15 | save.gd 未序列化 chapter_chest_claimed → 刷新后章节宝箱可重复领取（刷资源漏洞） | D 验收 + A 复核 | BUG | 🔴 | 已定位：save.gd 补读写 + 旧档兼容回归 | ✅ 闭环（B 修复 + F5 验证） |
| 16 | 敌方技能名显示为兜底 "enemy_skill"（ENEMY_SKILLS 无 id 键） | D 验收 + A 复核 | 显示完整性 | 🟡 | 已定位：_cast_skill 的 skill_id 兜底改用 skill.name | ✅ 闭环（B 修复 + F5 验证） |
| 17 | 部分 buff/状态变化未发 status_changed（_apply_buff 被直接调用路径） | D 验收 + A 复核 | 显示完整性 | 🟡 | 已定位：_apply_buff 内补发 + _tick_unit_timers 补发 | ✅ 闭环（B 修复 + F5 验证） |
| 18 | 主线"挑战第 N 关"按钮不可点（challenge_button 未连接 / 与旧"进入战斗"按钮重复） | D 复核 | 交互 | 🟡 | 已裁决：C 微补丁——连接 challenge_button 并移除旧按钮（一行级，不动契约） | 已派单 C |
| 19 | ENEMY_SKILLS 模板缺 "id" 字段（与机娘技能不一致，数据规范） | D 复核 | 数据规范 | 🟢 | 非紧急：随下次 data.gd 变更批次补 id 键 | 待办 |
| 20 | 满级时升级按钮未置灰/无提示（观察3，C 一行级） | D 复核 | 体验 | 🟡 | 择机处理：随下次 C 批次补"已满级"置灰提示 | 待办 |
| 21 | "被控不加能量"是否含受击需澄清（观察2） | D 复核 | 规则澄清 | 🟡 | P 澄清（2026-08-25）：**含受击**（被控期间受击也不加能量）——当前 game.gd `_gain_energy` 统一入口已符合，纯措辞确认；契约 v0.12 §3.9 ④ 已补"（含受击）"、设计文档 v0.19 §8.3 同步措辞 | ✅ 闭环（无代码改动） |

> D 判定：v0.10/v0.11 通过（红线零违反、旧档兼容、上轮 🟡 已修复），可发布。观察 1/4 为清理/历史项。v0.8 遗留观察项（battle_view 跨波状态聚合）续留。

## v0.11 新增问题（用户实测反馈 + A 复核）

| # | 问题 | 来源 | 类型 | 优先级 | 裁决 | 状态 |
|---|---|---|---|---|---|---|
| 22 | 战斗"前后排"轴与设计不符：设计文档 §1.5 规定"靠近敌方一侧为前排"（我方最右列/敌方最左列先接敌），实现却把"前排"做成了**上方行 row0**（敌方坦克排 row0、目标选择 row 小优先）→ 玩家看到"左上/上方角色先掉血"，位置感违和；遗留："追击同排"的"排"语义存疑 | 用户实测 + A 复核 | 规则偏差 | 🟡 | 方案 A（v0.11）：game.gd 改轴——敌方坦克排 col0、目标选择"靠近敌方一侧优先"、front/back AOE 按列、默认阵型右列优先；**遗留"追击同排"P 澄清（v0.12）= 同一列**，game.gd 追击改按 col 匹配 | ✅ 闭环（v0.11 前排轴 + v0.12 追击同列均 B 交付 + A 复核 + Godot headless 校验通过；待用户 F5 实测 + 重导出 Web 生效） |
| 23 | game.gd `_start_dungeon_battle`（约 1791 行）误用 `tier_cfg.waves`——档位字典（Data.DUNGEONS[kind].tiers[tier]）无 `waves` 键，`waves` 在**副本级** `Data.DUNGEONS[kind].waves`（按档位索引）→ 秘境挑战进战斗报 "Invalid access to property or key 'waves'"、敌方无法生成 | B 交付自测阻塞报告 + A 复核 | BUG | 🔴 | 退回 B：改 `battle.pending_waves.append(Data.DUNGEONS[kind].waves[tier])`（整波作为一项，与主线 pending_waves 结构一致） | ✅ 闭环（B 修复 + A 复核 + Godot headless 校验通过） |
| 24 | 秘境战斗与 battle_view 衔接缺失（D 验收 🟡×3）：① 标题仍显示"第 N 关·战斗"；② 失败重试调 Game.start_battle(level) 而非 start_dungeon；③ 未 connect dungeon_reward，秘境通关无奖励反馈 | D 验收（v0.13） | UI 适配 | 🟡 | C 端按 Game.battle.mode 适配 battle_view（秘境标题=副本名+档位、重试改调 start_dungeon、connect dungeon_reward 显示反馈） | ✅ 闭环（C 交付 + A 复核 + Godot headless 校验通过） |
| 25 | 秘境胜利写入 last_clear，主界面"上次通关"误显示为主线关卡首通（D 验收问题 4 + A 观察项） | D 验收 + A 复核 | 规则/显示 | 🟡 | B：`_resolve_dungeon_victory` 不再写 last_clear（last_clear 仅主线使用；秘境反馈走 dungeon_reward） | ✅ 闭环（B 交付 + A 复核 + Godot headless 校验通过） |
| 26 | 秘境重试体力不足无反馈：失败返还体力后若仍不足，battle_view 重试按钮点了无反应（start_dungeon 校验失败静默 return） | D 复核（阶段 3） | 体验 | 🟡 | C 一行级：battle_view 重试前判断 `Game.get_stamina() < dungeon_cost(kind,tier).stamina` 则隐藏重试按钮并提示"体力不足"（或 start_dungeon 失败经 battle_prompt 提示） | ✅ 闭环（C 交付 + A 复核 + Godot headless 校验通过） |
| 27 | settings 音量读档未钳制（music_volume/sfx_volume 可越界 0..1） | D 复核（阶段 3） | 健壮性 | 🟡 | B 一行级：save.gd 读档时对音量钳 0..1 | ✅ 闭环（B 交付 + A 复核 + Godot headless 校验通过） |
| 28 | game.gd 第 362 行 `var equipped_data`（称号读档）与第 279 行（v0.14 穿戴读档）同名重复声明 → Game autoload 编译失败（Parse Error）、连锁 Save 编译失败、游戏无法启动 | C 提请 + A 复核（Godot 日志证实） | BUG | 🔴 | 退回 B：第 362 行变量改名 `title_equipped_data`（C 临时副本验证可行）；修复后需重导出 Web + 删旧档再测 | ✅ 闭环（B 修复 + A 复核 + Godot headless 复测通过：EXIT=0 零脚本错误） |
| 29 | "秘境达人"称号不可解锁：`_achievement_progress`（约 2922 行）无 "dungeon" 分支（仅支持 story_cleared/summon_count/power/tower/collection），Data.TITLES 秘境条件 type 无法判定 → 4 个称号中 1 个永不达标 | D 复核（阶段 4） | 规则/数值 | 🟡 | B 一行级：`_achievement_progress` 加 "dungeon" 分支（返回 dungeon_cleared 累计档数）；或 Data 称号条件 type 改为已支持类型 | 已派单 B |
