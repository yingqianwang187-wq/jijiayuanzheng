# 验收报告 · D（测试官 QA）— 首轮只读验收

- 轮次：v0.4 代码层只读验收（D 本机无 Godot，未实际运行；运行时结论待 F5 回归后由 D 复核）
- 结论（D）：代码层面四项验收全部达标、**契约零违规**；需 A 安排 F5 回归。

## 必补测项（需真人 F5 执行，本次无法完成）
1. 信号时序与场景切换表现（进入战斗 / 返回主界面的首屏快照与信号衔接）
2. `user://save.json` 实际读写
3. **刷新页面后四项进度还在**（金币 / 待收获 / 机娘经验 / 关卡进度——任务 8 硬验收）
4. 离线计时：挂机 10 秒 → 关游戏 → 等 30 秒 → 重开，看待收获是否计入离线时长

## 待裁决规则（需 B 或 P 给出规则后落实）
- **#3 返回主界面是否中止战斗**：现实现为"返回仅切场景、战斗计时器仍跑"（战斗可能在主界面后台打完）。A 建议：**返回 = 中止本局**，新增 `Game.stop_battle()` 入口（契约 v0.5）。
- **#4 阵亡机娘是否得经验**：现实现为"全体上阵机娘 +经验"。A 建议：**出战即算（含本局阵亡）**，MVP 从简不惩罚；后期 5v5 再考虑策略性（存活才得）。

## 体验小修建议（D）
- **#1** 升级按钮加"禁用态 + 提示"（金币/经验不足时置灰并显示原因）——需新增 Game 只读费用入口 `upgrade_cost(id)` / `upgrade_exp_cost(id)`（顺带消除老观察项"UI 自行换算"）。
- **#2** 主界面补"最近一次通关奖励"快照消息（重入时显示上次通关奖励）——需 Game 记录内存态 `last_clear`（不入档）。

## 状态
- A 已归档（2026-08-24，v0.4 轮次）
- 三项裁决已确认（返回=中止、阵亡得经验、本轮做体验小修）→ 契约已升 **v0.5**，B/C 派单中（见 docs/变更单.md）
- 下一步：v0.5 补丁交付 → 综合 F5 回归（含上方 4 项补测）→ D 复核

---

## v0.5 静态复核（D，2026-08-24 第二轮）
- 结论：v0.5 四项改动与契约 v0.5 **逐字对齐**、接口方向正确、UI 红线零违反，**静态复核通过**。
- v0.4 遗留问题本轮全部闭环：首通消息丢失 ✓、升级无反馈 ✓、后台战斗 ✓、阵亡经验 ✓、费用换算 ✓。
- **仍待实测（必须由真人 F5 补齐，D 无法替代）**：
  1. 进战斗 → 点"返回" → 确认战斗立即中止（回主界面后不再自动出首通/奖励）；
  2. save.json 读写与刷新后进度；
  3. 离线计时待收获；
  4. 升级按钮禁用提示 + 上次通关消息实际显示。
- **新建议（已裁决：本轮顺手修，C 小补丁，不动契约）**：
  - #1 消息 label 冲突："上次通关"改用**独立标签**展示（message_label 保留给即时消息）；
  - #2 "+0 文案"：非首通重复通关不显示"奖励 +X 金币"（无金币奖励），仅首通显示。

---

## 终态（2026-08-24）
- C 体验收尾补丁已交付（#11 独立标签 / #12 非首通文案），**用户手工 F5 回归全部通过**。
- MVP（阶段 0）结论：**可交付**。剩余收尾：备份 → GitHub → 导出 Web。
- 遗留：指挥官经验/等级（阶段 3）；UI 满血值/exp_next 快照换算观察项（不阻塞，留待后续可选优化）。

---

## v0.13 静态复核（D，2026-08-26 第三轮）

- **结论：通过（静态）**——三大系统（体力/秘境/背包/开箱）的接口、信号、存档、数值全部达标，**红线零违反、旧档兼容**；无 🔴 阻断。
- **🟡 问题 4 个**（均为秘境战斗与既有 battle_view 的衔接缺失，C 端适配）：
  1. 秘境战斗标题仍显示"第 N 关·战斗"（未按 battle.mode 区分）；
  2. 秘境失败"重试"调 Game.start_battle(level) 而非 Game.start_dungeon(kind, tier)；
  3. battle_view 未 connect dungeon_reward，秘境通关无奖励反馈；
  4. 秘境胜利写入 last_clear（主界面"上次通关"误显示为主线关卡首通）——B 或 C 修。
- **🟢/注释项 3 个**（随下次版本处理）：5 注释完善、6 equip/gem 材料占位属 3B 批次、7 规则确认（满体力可购买等）。
- **处理**：问题 1/2/3 → 派单 C（battle_view 按 Game.battle.mode 适配：秘境标题/重试/dungeon_reward）；问题 4 → 派单 B（秘境胜利不写 last_clear）。
- **F5 回归必测清单（D）**：① 体力自然恢复/买体力跨日重置；② 秘境首免→失败返还→重试→扫荡全流程；③ 开箱权重与入账；④ 扩容费用曲线；⑤ 刷新页面后体力/背包/箱子/秘境进度还在；⑥ 秘境战斗标题与重试按钮表现（问题 1~3 验证点）。

---

## 阶段 3 整体复核（D，2026-08-26 第四轮）

- **结论：通过（静态）**——阶段 3 全量（体力/秘境/背包/开箱/装备/宝石/强化/商城/设置）与契约 v0.15 **逐字对齐**：信号 11 / 入口 28 / 存档 13 三端齐全、红线零违反、旧档兼容、上轮 4 个 🟡（#24/#25）全部闭环、装备加成无溢出风险。**无 🔴**。
- **🟡 建议修复 2 个（均一行级）**：
  1. 秘境重试体力不足无反馈：失败返还体力后，若体力仍不足以重试，重试按钮点了无反应 → C 端 battle_view 在重试前判断 `Game.get_stamina() < dungeon_cost(kind,tier).stamina` 则隐藏重试并提示（或 start_dungeon 失败经 battle_prompt 提示）；
  2. settings 音量读档未钳制 → B 端 save.gd 读档时对 music_volume/sfx_volume 钳 0..1。
- **🟢 清理项 2 个**：问题 3/4（注释/后续批次）。
- **F5 回归必测清单（D）**：① 装备穿卸/合成/强化/镶嵌/拆卸/宝石合成全流程与属性变化；② 商城购买/限购/跨日刷新；③ 设置修改/导出/重置二次确认；④ 秘境装备/宝石副本掉真货；⑤ 刷新页面后装备/宝石/商城已购/设置都在；⑥ 问题 1 的复现路径。

---

## 阶段 4（4A+4B）复核（D，2026-08-26 第五轮）

- **结论：通过（静态）**——阶段 4 全量（爬塔/签到/任务/新手/图鉴/成就/称号/好感）与契约 v0.17 对齐：**信号 8 / 入口 16 / 存档 14 三端齐全**、红线零违反、**#28 修复确认**、历史回归通过。**无 🔴**。
- **🟡 建议修复 1 个**：
  1. **"秘境达人"称号不可解锁**：`_achievement_progress` 无 "dungeon" 分支（称号条件 type 用到秘境档数，进度函数不支持）→ 4 个称号中 1 个无法解锁。修法：B 一行级——`_achievement_progress` 加 "dungeon" 分支（返回 dungeon_cleared 累计档数），或 Data 称号条件 type 改为已支持类型；修后 4 个称号全部可解锁。
- **🟢 契约文档同步 4 处字段**（A 按 v0.15 先例对齐，不影响运行）。
- **F5 回归必测清单（D）**：① 爬塔推进/10 层大奖/30 层日限/失败重试；② 签到断签重置与 7 天奖；③ 每日任务钩子累计与档位领奖、跨日/周一重置；④ 新手 7 日逐日领奖与 power 判定；⑤ 图鉴档位奖励、12 成就判定与领奖、4 称号解锁/佩戴/属性、好感送礼/出战 +1/满级加成；⑥ 刷新后 14 字段进度都在；⑦ "秘境达人"称号验证点。

---

## 阶段 4 + 经验系统 + 4U 整体复核（D，2026-08-26 第六轮）

- **结论：通过（静态）**——v0.16~v0.19 + 4U 全量：**无 🔴 / 无 🟡**；#29（秘境达人称号）确认已修复（_achievement_progress "dungeon" 分支，target 4 可达成）。
- **建议 2 项（非阻塞）**：① A 按 v0.17 先例对齐契约 2 处字段——`commander_ten_rewarded` 契约写数组、B 实现为 int（已对齐，见契约变更记录 v0.19）；② Novice 后续改为覆盖层统一交互（当前为整页切换）。
- **F5 回归必测清单（D）**：① 经验池化（战斗/秘境/挂机入池、升级扣池、旧档迁移、按钮禁用提示）；② 4U 页面切换/覆盖层返回/右上角常驻/引导高亮定位；③ 指挥官升级与每 5 级 +10 券、活动领奖；④ 阶段 4 各界面刷新后进度都在；⑤ 主城/主线/机娘页交互。
- 问题清单 #23~#32 状态与代码一致（#29/#31/#32 已闭环）。

---

## 阶段 5 整体复核（D，2026-08-26 第七轮）

- **结论：通过（静态）**——阶段 5 九系统（限定池/皮肤/每日BOSS/排行榜/远征/生存/家园/转盘/节日）与契约 v0.22 逐条对齐，battle.mode 五分支完整，**无 🔴 / 无 🟡**。
- **建议 2 项（A 已处理）**：① 契约 2 处字段对齐（get_rank_info 返回 {power, tower, boss_damage, story_progress}、get_festival_info 每项 active/done/claimed，v0.17 先例）；② contract.gd 文件头注释修正（版本号 v0.11 → v0.22、设计文档 v0.1 → v0.23）。
- **F5 回归必测清单（D）**：① 限定池抽取/UP/独立保底；② 每日 BOSS 伤害结算与档位奖励、排行榜四榜；③ 远征派遣/离线计时/到期领取；④ 生存多波次/每波重置/结算；⑤ 家园互动次数/好感；⑥ 转盘免费+续转、节日活动日期匹配；⑦ 刷新后 9 字段进度都在。
- 问题清单 #23~#34 状态与代码一致（阶段 5 相关项已闭环）。
