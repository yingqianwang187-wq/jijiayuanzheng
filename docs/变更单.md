# 变更单 · v0.4（挂机点收获 + 机娘经验系统）

- 依据：docs/设计文档.md v0.2（策划官 P 交付，已验收）、docs/契约.md v0.4（契约官 A）
- 派单顺序：**先 B、后 C**（C 依赖 B 的新信号/入口；B 交付后再派 C）
- 状态：B ✅ 已交付并验收（后端就绪）→ C ✅ 已交付并验收（收获按钮/待收获显示/经验显示）

---

## B 变更单（scripts/data.gd / scripts/save.gd / scripts/game.gd）

【变更单】契约 v0.4 落地：挂机点收获 + 机娘经验系统（总指挥 A 派单）
背景：设计文档已升 v0.2，契约已升 v0.4。请按契约 v0.4 修改你的三个文件。
【必读】docs/契约.md（§1.2 / §1.3 / §3.2 / §3.5 / §3.6 / 变更记录 v0.4）、docs/设计文档.md v0.2（§8.4 数值）、scripts/contract.gd（只读，新增信号已声明）
【只允许修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md

1. data.gd：
   - 挂机金币基础 1 → 0.5（IDLE_GOLD_BASE），每过 1 关 ×1.25 不变；
   - 敌人数值按设计文档 v0.2 §8.4 表更新（攻 14/17/21/25/31、血 120/146/179/218/266，每关 1 敌）；
   - 新增升级经验曲线常量：UPGRADE_EXP_BASE := 15、UPGRADE_EXP_GROWTH := 1.25（所需经验 = roundi(15 × 1.25^(N-1))）；
   - LEVELS[level] 增加 victory_reward_exp = 10 + level × 3（13/16/19/22/25）；
   - 只放数据不放逻辑。
2. save.gd：
   - 存档形状改为 {gold, mechs{level, exp}, unlocked_level, first_cleared, idle_pending, idle_last_time}；
   - save_game() 写 mechs.exp、idle_pending、idle_last_time；load_game() 读回并钳制（exp ≥ 0、idle_pending ≥ 0、idle_last_time 非法取当前时间）；默认值：exp 0、idle_pending 0、idle_last_time 取当前时间戳（Time.get_unix_time_from_system()）。
3. game.gd：
   - 挂机改"点一下收获"：新增状态 idle_pending（float 累积额）、idle_last_time（时间戳）；启动时按"现在 - idle_last_time"把离线时长补入 idle_pending（离线收益）；每秒节拍把 rate × 1.0 累入 idle_pending 并发 idle_rewards_updated(roundi(idle_pending))；新增 collect_idle()：gold += int(idle_pending)，idle_pending = 0，idle_last_time = now，发 gold_changed + idle_rewards_updated(0)，并 Save.save_game()；待收获每 5 秒节流存档（沿用 Data.SAVE_THROTTLE_INTERVAL）；
   - 机娘经验：新增 mech_exp（{id: int}）；胜利时全体上阵机娘 + victory_reward_exp，发 mech_exp_updated(id, exp, exp_next)（exp_next = 当前等级升下一级所需经验）；失败不发经验；
   - upgrade(mech_id)：校验金币 ≥ 金币费用 且 经验 ≥ 所需经验；两者都够才扣（经验扣减、余额保留），等级 +1，发 gold_changed / mech_girl_updated / mech_exp_updated，Save.save_game()；
   - get_save_snapshot() 返回形状同步 v0.4（含 mechs.exp、idle_pending、idle_last_time）；
   - 删除旧注释中"挂机金币每秒自动入账"的表述。
【验收】
① 挂机攒"待收获"，点收获一次性入账（关游戏再开，离线时长也补入）；
② 升级需金币 + 经验，经验不足无法升级；
③ 胜利给经验、失败不给；非首通重复胜利也给；
④ 刷新页面后待收获金额、机娘经验都在。
【汇报】末尾输出【交付说明】。

---

## C 变更单（scenes/Main.tscn / scripts/main_ui.gd）

【变更单】契约 v0.4 落地：主界面加"收获"与"经验"显示（总指挥 A 派单）
背景：契约 v0.4，B 已按 v0.4 交付（若 B 未交付，先等 B 完成）。
【必读】docs/契约.md（§3.5 / §3.6 / 变更记录 v0.4）、scripts/contract.gd（只读，新增信号 idle_rewards_updated / mech_exp_updated）
【只允许修改】scenes/Main.tscn、scripts/main_ui.gd
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd、scenes/Battle.tscn、scripts/battle_view.gd

1. main_ui.gd：
   - 新增 connect：idle_rewards_updated(amount) → 显示"待收获：N 金币"；mech_exp_updated(id, exp, exp_next) → 机娘行追加显示"经验 exp/exp_next"；
   - 新增"收获"按钮 → Game.collect_idle()（结果由 gold_changed / idle_rewards_updated 信号回发，不依赖返回值）；
   - 首屏快照补读 Game.idle_pending 与 Game.mech_exp（只读）；
   - 保持红线：不写数值、不 emit。
2. Main.tscn：新增"待收获"标签（节点名 idle_rewards_label）与"收获"按钮（节点名 collect_button），与脚本引用一一对应。
【验收】
① 待收获金额随信号刷新，点收获金币到账、待收获归零；
② 机娘行显示经验进度（exp/exp_next）；
③ 主界面无数值赋值、无 emit。
【汇报】末尾输出【交付说明】。

---

## v0.5 变更单（D 首轮验收裁决：返回中止战斗 + 阵亡得经验 + 体验小修）

### B 变更单（仅 scripts/game.gd；data.gd / save.gd 不动）

【变更单】契约 v0.5：新增 3 个入口 + last_clear 内存态（总指挥 A 派单）
【必读】docs/契约.md（§1.3 / §3.6 / 变更记录 v0.5）
【只允许修改】scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、scripts/data.gd、scripts/save.gd
1. 新增 `stop_battle()`：若 battle.active，置 false 并停止 battle_timer（幂等，可重进重打）；
2. 新增只读 `upgrade_cost(mech_id) -> int` 与 `upgrade_exp_cost(mech_id) -> int`：分别返回该机娘当前等级升下一级所需金币/经验（复用内部 _upgrade_cost / _upgrade_exp_cost，不修改数值）；
3. 新增内存态 `last_clear`（Dictionary，如 {level, first_clear, reward}），在 _resolve_victory 胜利结算时记录（不入档、不持久化）；
4. 阵亡得经验：现有实现（全体出战机娘 + victory_reward_exp）已满足契约 v0.5 §1.3"含阵亡者"，若循环遍历的是 Data.MECH_GIRLS 请改为遍历本局出战名单（battle.mechs 的 id），语义更精确（MVP 二者等价）。
【验收】① stop_battle 后战斗停止、可重进重打；② 两个费用入口与内部逻辑一致；③ 胜利后 last_clear 记录本关信息。
【汇报】末尾输出【交付说明】。

### C 变更单（scripts/battle_view.gd、scripts/main_ui.gd；场景无需新增节点）

【变更单】契约 v0.5：返回中止战斗 + 升级按钮禁用提示 + 上次通关消息（总指挥 A 派单）
【必读】docs/契约.md（§1.3 / §3.6 / 变更记录 v0.5）
【只允许修改】scripts/battle_view.gd、scripts/main_ui.gd
【只读】scripts/game.gd、scripts/contract.gd、scenes/Main.tscn、scenes/Battle.tscn
1. battle_view.gd `_on_back_pressed`：先调 `Game.stop_battle()`，再切换场景到 Main.tscn；
2. main_ui.gd 升级按钮：金币/经验不足时禁用并提示原因——用只读入口 `Game.upgrade_cost(id)` / `Game.upgrade_exp_cost(id)` 判断（不再自行换算费用）；在 gold_changed / mech_exp_updated / mech_girl_updated 信号到达时刷新所有机娘行的按钮状态（提示可复用 message_label）；
3. main_ui.gd 上次通关消息：首屏快照时若 `Game.last_clear` 非空，显示"上次通关：第 N 关（首通/重复）奖励 +X 金币"。
【验收】① 返回主界面后战斗不再推进（回到主界面等几秒看无新信号/无自动结算）；② 金币/经验不足时升级按钮置灰并显示原因，攒够后恢复可用；③ 重入主界面显示上次通关奖励消息。
【汇报】末尾输出【交付说明】。

---

## v0.6 变更单（策划官 P 设计文档 v0.3：挂机同产经验、经验分个人条/全局余额、右上角余额展示）

### B 变更单（scripts/data.gd / scripts/save.gd / scripts/game.gd）

【变更单】契约 v0.6：挂机同产经验 + 经验分两路 + 右上角余额数据（总指挥 A 派单）
【必读】docs/设计文档.md v0.3（§3.2 / §3.3 / §3.7 / §8.4）、docs/契约.md（§1.2 / §3.2 / §3.5 / §3.6 / §3.7 / 变更记录 v0.6）、scripts/contract.gd（只读，v0.6 已声明新信号）
【只允许修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md
1. data.gd：新增挂机经验比例常量 `IDLE_EXP_RATIO := 0.5`（挂机经验 = 待收获金币 × 0.5，设计文档 §8.4"1 金币 = 0.5 经验"）；只放数据不放逻辑。
2. save.gd：存档形状改为 `{gold, exp_balance, idle_pending, idle_pending_exp, idle_last_time, unlocked_level, first_cleared, mechs{level, exp}}`；写/读并钳制（exp_balance ≥ 0、idle_pending ≥ 0、idle_pending_exp ≥ 0、idle_last_time 非法取当前时间）；默认值：exp_balance 0、idle_pending 0、idle_pending_exp 0。
3. game.gd：
   - 新增状态：`idle_pending_exp`（float）、`exp_balance`（int）。
   - 挂机节拍：idle_pending += rate、idle_pending_exp += rate × Data.IDLE_EXP_RATIO；发 `idle_rewards_updated(roundi(idle_pending), roundi(idle_pending_exp))`；离线时长同样按 rate 与 rate×IDLE_EXP_RATIO 补入二者；待收获金币/经验每 5 秒节流存档。
   - `collect_idle()`：gold += roundi(idle_pending)；exp_balance += roundi(idle_pending_exp)；清零二者；发 gold_changed、exp_balance_updated(exp_balance)、idle_rewards_updated(0,0)；存档。
   - 升级：`upgrade(mech_id)` — 经验部分改为**先扣个人条（mech_exp[id]）、不足自动从 exp_balance 补、都不足则失败（不扣金币、不发信号）**；成功则扣金币 + 经验（个人条与余额分别扣减）、等级+1、发 gold_changed / mech_girl_updated / mech_exp_updated（个人条）/ 若用了余额则发 exp_balance_updated；存档。
   - 战斗胜利经验：仍进**个人经验条**（mech_exp[id] += victory_reward_exp，发 mech_exp_updated），不进余额——保持不变。
   - 启动读档后发初始信号：gold_changed、mech_girl_updated、mech_exp_updated、exp_balance_updated、level_progress_changed、idle_rewards_updated(roundi(idle_pending), roundi(idle_pending_exp))。
   - `get_save_snapshot()`：返回形状同步 v0.6（含 exp_balance、idle_pending_exp、mechs.exp）。
【验收】① 挂机同时累计待收获金币与经验；点收获金币入账、经验入全局余额，右上角两个余额都更新；② 战斗胜利经验进机娘个人条（不进余额）；非首通重复胜利也给；③ 升级先扣个人条、不足自动从余额补；个人条+余额都不足则不能升级（无反应/提示）。
【汇报】末尾输出【交付说明】。

---

### C 变更单（scenes/Main.tscn / scripts/main_ui.gd）

【变更单】契约 v0.6：右上角余额展示（总指挥 A 派单）
【必读】docs/契约.md（§3.5 / §3.7 / 变更记录 v0.6）、scripts/contract.gd（只读）
【只允许修改】scenes/Main.tscn、scripts/main_ui.gd
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd
1. main_ui.gd：
   - 新增 connect：`exp_balance_updated(balance)` → 右上角"经验余额"显示；把既有 `idle_rewards_updated(amount)` 处理函数改为双参数 `(gold, exp)`，用其刷新右上角"待收获"部分（金币(+gold) / 经验(+exp)）。
   - 右上角余额展示（契约 §3.7）：金币余额 = Game.gold + Game.idle_pending；经验余额 = Game.exp_balance + Game.idle_pending_exp；显示如 `金币 1,234 (+567)` / `经验 890 (+280)`；随 gold_changed / idle_rewards_updated / exp_balance_updated 与首屏快照刷新。
   - 首屏快照补读：Game.exp_balance、Game.idle_pending_exp。
   - 收获按钮仍调 Game.collect_idle()；保持红线：不写数值、不 emit。
2. Main.tscn：右上角新增两个展示标签（如 `gold_balance_label`、`exp_balance_label`），与脚本引用一一对应。
【验收】① 右上角常驻显示金币/经验余额，含"待收获"部分（+数字），刷新后正确；② 点收获后两个余额并入、待收获归零；③ 主界面无数值赋值、无 emit。
【汇报】末尾输出【交付说明】。

---

## v0.7 变更单（阶段 1 抽卡：钻石 + 抽卡 + 碎片 + 拥有/上阵）

### B 变更单（scripts/data.gd / scripts/save.gd / scripts/game.gd）

【变更单】契约 v0.7：阶段 1 抽卡（钻石、抽卡逻辑、卡池数据、碎片、拥有/上阵）（总指挥 A 派单）
【必读】docs/设计文档.md v0.4（§2.4 / §4 / §9 阶段 1）、docs/契约.md（§1.2 / §1.4 / §3.2 / §3.5 / §3.6 / §3.8 / 变更记录 v0.7）、scripts/contract.gd（只读，v0.7 已声明新信号）
【只允许修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md
1. data.gd：新增 5 位机娘（千夏·SR辅助 10/130/12/7、莲·SR坦克 14/190/15/6、月见·SR狙击 20/100/6/12、绯·SSR近战爆发 26/160/10/13、星澜·SSR远程炮击 30/130/8/11），加入 MECH_GIRLS（字段同现有：name/rarity/role/base_*_/upgrade_cost/growth）；升级曲线沿用现有。新增卡池与抽卡数据：`SUMMON_POOLS`（常驻池 = 全部 7 位 id；新手池 = 星澜保底位 + 千夏/莲 + 小钰/阿岚）、抽卡常量（单抽 300、十连 2800、新手半价、SSR3%/SR17%/R80%、80抽保底、十连保底SR、新手池半价次数5、首十连保底星澜）、碎片换算（R10/SR20/SSR50）。只放数据不放逻辑。
2. save.gd：存档形状增加 `diamond`、`summon_ticket`、`fragments`（{id:int}）、`owned_mechs`（{id:true}）、`pity`（{permanent_ssr:int, permanent_since_ssr 等，或简化为各池保底计数}）、`novice_free_pull`(bool)、`novice_pool_left`(int)；读/写钳制；默认值（diamond 0、fragments 空、owned_mechs 含小钰/阿岚、novice_free_pull true、novice_pool_left 5、pity 计数 0）。读失败返回默认值。
3. game.gd：
   - 新增状态：diamond、fragments、owned_mechs、pity、novice_free_pull、novice_pool_left。
   - 首通奖励：首通发钻石（按 Data.LEVELS 首通奖励表"钻石"列），发 diamond_changed；Level_cleared 时一并。
   - 抽卡入口：`summon(pool, times)` — 校验（池存在、钻石/免费次数足够、新手池半价次数），扣钻石（新手半价/开局免费十连），按概率抽 times 次，处理保底（80 抽保底 SSR、十连保底 SR、新手首十连保底星澜），结果生成 entries（每项 {id, rarity, is_new, fragments}）；新机娘加入 owned_mechs（发 owned_mechs_updated）；重复机娘→碎片（发 fragments_updated）；扣完发 diamond_changed + gacha_result(entries)；存档。新手池半价次数递减；免费十连用后置 false。
   - 只读入口：`summon_cost(pool, times) -> int`（含新手半价/免费判断）、`summon_pity_info(pool) -> Dictionary`、`get_owned_mechs() -> Array`。
   - 拥有与上阵：初始拥有小钰/阿岚；战斗上阵 = owned_mechs 中按 Data.MECH_GIRLS 顺序取前 ≤5 位（start_battle 改为遍历 owned_mechs，上限 5）；敌方仍每关 1 敌（MVP）。
   - get_save_snapshot() 形状同步（含 diamond、fragments、owned_mechs、pity、novice_*）。
【验收】① 首通拿钻石、钻石显示正确；② 抽卡能出机娘/碎片、SSR 保底与十连保底生效、新手半价与首十连保底星澜生效；③ 重复机娘转碎片、碎片计数正确；④ 抽到新机娘可上阵（上阵数 ≤5、开局 2 位 2v1）；⑤ 刷新页面钻石/碎片/拥有/保底都在。
【汇报】末尾输出【交付说明】。

---

### C 变更单（scenes/Main.tscn / scripts/main_ui.gd + 新增 scenes/Gacha.tscn / scripts/gacha_ui.gd）

【变更单】契约 v0.7：抽卡界面 + 钻石/碎片展示（总指挥 A 派单）
【必读】docs/契约.md（§3.5 / §3.6 / §3.8 / 变更记录 v0.7）、scripts/contract.gd（只读）
【只允许创建/修改】scenes/Gacha.tscn、scripts/gacha_ui.gd、scenes/Main.tscn、scripts/main_ui.gd
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd、scenes/Battle.tscn、scripts/battle_view.gd
1. 抽卡界面（scenes/Gacha.tscn + gacha_ui.gd）：常驻/新手池切换；单抽/十连按钮；显示所需钻石（用 Game.summon_cost 只读）；连接 `gacha_result` 展示抽到的机娘（新机娘标记、碎片数）；连接 `diamond_changed` 显示钻石；`owned_mechs_updated` 刷新拥有列表；按钮只调 `Game.summon(pool, times)`；红线：不写数值、不 emit。
2. main_ui.gd：主界面加"抽卡"入口按钮（跳 Gacha.tscn）；连接 `diamond_changed` 显示钻石；连接 `fragments_updated`、`owned_mechs_updated` 刷新机娘列表（只读）。
3. Main.tscn：新增"抽卡"按钮（进 Gacha.tscn）；右上角可加钻石显示。
【验收】① 能进抽卡界面、切池、单抽/十连、出结果展示、保底信息可见；② 钻石/碎片随抽卡正确变化；③ 新机娘入拥有列表、可上阵；④ 抽卡界面与主界面无数值赋值、无 emit。
【汇报】末尾输出【交付说明】。

---

> 【A 复核结论（v0.7·B）】4 条决策全部通过：① `novice_first_ten_done` 入档（防刷新重复保底）；② SSR 保底为全局计数、`summon_pity_info` 的 pool 参数暂不影响；③ 免费十连优先、不耗新手半价次数；④ 新手池普通抽按 3%/17%/80%、SSR 档=星澜、首十连保底为额外强制位。契约 §3.2 存档形状已补列 v0.7 新字段。

---

## v0.8 变更单（阶段 2 战斗系统 2.0）

### B 变更单（scripts/data.gd / scripts/save.gd / scripts/game.gd）

【变更单】契约 v0.8：战斗 2.0（九宫格 5v5、全自动、技能/能量/克制/暴击闪避/状态/波次/BOSS、加速/扫荡/星级/阵型）（总指挥 A 派单）
【必读】docs/设计文档.md v0.14（§2.5 / §2.5.1 / §2.5.2 / §8.3 / §10.10 / 附录 B）、docs/契约.md（§1.3 / §3.5 / §3.6 / §3.9 / 变更记录 v0.8）、scripts/contract.gd（只读，v0.8 已声明新信号）
【只允许修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md
1. data.gd：
   - 机娘表扩充至 **30 位**（§2.2/§2.4/§2.4.1），每机娘增加：`class`（tank/fighter/assassin/archer/mage/support）、`skills`（`{passive, small:[2], ult}`，含效果/倍率/CD/概率，数值以设计文档 §2.5.1/§2.5.2 为准）。
   - 新增敌人技能模板库（设计文档 §10.10，普通 1 技 / 精英 2 技 / BOSS 2 小技+大招）。
   - 新增章节结构（§1.3）：每章关卡数、每关波数（1~3）、最后 1 关 BOSS 标记与配置。
   - 新增战斗常量：克制环、暴击 10%/暴伤 150%/闪避 5%、状态表（3 轮不叠加）、能量（命中/受击 +10、100 满）、伤害公式常量（减伤下限 30%）、战力公式（攻×4+血+防×6+速×5）、60 轮上限、星级规则。
2. save.gd：存档新增 `formation`（当前阵型，9 格选 5）、`formation_presets`（2~3 套）、`level_stars`（各关星级）、`cleared_boss`（章节 BOSS 通关，解锁下一章）、`accelerate_on`（可选）；旧档自动补齐缺失字段（v0.14 兼容迁移，读失败返回默认值）。
3. game.gd（战斗 2.0 重写）：
   - `start_battle(level)` 按当前阵型布阵（我方 5 格）、敌方按模板排阵（前排肉后排输出）、生成能量=0、读取波次。
   - 每轮：按速度排序→我方/敌方行动→自动普攻/小技能(CD)/大招(能量满)→目标优先同列最近→处理暴击/闪避/克制/状态→结算伤害（攻击×倍率−防御，下限 30%）→发 skill_cast/energy_changed/status_changed/battle_prompt→波次推进→Boss 关特殊。
   - 胜负：敌方全灭胜（发 level_cleared + battle_star + 星奖）；我方全灭败（battle_failed）；**60 轮超时**按剩余血量百分比判。
   - `set_formation/get_formation/save_formation_preset/load_formation_preset`、`toggle_accelerate`（2x）、`sweep_level`（扫荡已通关）、`get_power`（战力公式）。
   - 战斗内 2x 加速：Timer 间隔减半；扫荡：跳过战斗直接结算奖励。
   - 阵型/星级/BOSS 进度入档。
【验收】① 能布阵（9 选 5）并保存/读档多套阵型；② 5v5 九宫格战斗全自动跑通，技能/能量/克制/暴击/闪避/状态/波次/BOSS 全部生效；③ 60 轮超时判负、1~3 星评价、星奖/章节宝箱；④ 2x 加速与扫荡生效；⑤ 刷新页面阵型/星级/BOSS 进度都在。
【汇报】末尾输出【交付说明】。

---

### C 变更单（scenes/Main.tscn / scripts/main_ui.gd + 新增 scenes/Formation.tscn / scripts/formation_ui.gd + 改 scenes/Battle.tscn / scripts/battle_view.gd）

【变更单】契约 v0.8：布阵界面 + 战斗界面 2.0（总指挥 A 派单）
【必读】docs/契约.md（§3.5 / §3.6 / §3.9 / 变更记录 v0.8）、scripts/contract.gd（只读）
【只允许创建/修改】scenes/Formation.tscn、scripts/formation_ui.gd、scenes/Battle.tscn、scripts/battle_view.gd、scenes/Main.tscn、scripts/main_ui.gd
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd
1. 布阵界面（Formation.tscn + formation_ui.gd）：3x3 九宫格，选择已拥有机娘放置（≤5 格）；调用 `Game.set_formation`，显示 `get_formation`；预设保存/切换（`save/load_formation_preset`）；connect `formation_changed`；红线：不写数值、不 emit。
2. 战斗界面（Battle.tscn + battle_view.gd）重写 2.0：九宫格双方站位显示；connect `skill_cast / energy_changed / status_changed / wave_changed / battle_prompt / battle_star / battle_tick / mech_girl_updated / enemy_updated / level_cleared / battle_failed`；显示技能释放名、能量条、状态图标、波次、战斗提示分色（命中/暴击/闪避/击杀/技能/治疗/护盾）、星级结算、简版伤害统计、2x 加速按钮（`toggle_accelerate`）、扫荡按钮（`sweep_level`）；只读显示、不写数值、不 emit。
3. main_ui.gd / Main.tscn：主界面加"布阵"入口（跳 Formation.tscn 或底部导航）；三星评价/章节宝箱提示（connect battle_star / level_cleared）；右上角可加体力显示（阶段 3 再做）。
【验收】① 能进布阵界面、放 5 格、保存/切换多套阵型；② 战斗界面 2.0：九宫格、技能名、能量条、状态、波次、分色提示、星级、统计、2x 加速、扫荡全显示；③ 阵型/星级/加速/扫荡均只调 Game 入口、无数值赋值、无 emit。
【汇报】末尾输出【交付说明】。

---

> 【A 复核结论（v0.8·B）】战斗 2.0 引擎交付验收通过；4 条决策全部通过：① `chapter_chest_claimed` 入档（防刷新重复领章节宝箱，同 novice_first_ten_done 先例）；② 敌方 5v5 数值下调为推荐值（L5 BOSS 攻 30/血 600，标注【待确认】可调）、技能倍率按附录 C（R≤600%/SR≤750%/SSR≤900%）核验未超模；③ 扫荡给上阵机娘经验（与战斗胜利语义一致）；④ 战前被动 aura（鸦/洛）布阵时一次性结算到 atk、战斗局部不持久化。契约 §3.2 存档形状已增 v0.8 字段。

---

## v0.9 变更单（战斗 2.0 修复 + 主线玩法调整，契约 v0.9）

### B 变更单（scripts/game.gd / scripts/save.gd）

【变更单】契约 v0.9：3 处 D 验收问题修复 + 主线规则落地（总指挥 A 派单）
【必读】docs/设计文档.md v0.15（§1.3 主线推进）、docs/契约.md（§1.3 / §3.6 / §3.9 / 变更记录 v0.9）
【只允许修改】scripts/game.gd、scripts/save.gd
【只读】scripts/contract.gd、scripts/data.gd、docs/契约.md
A. 三处 D 验收修复：
1. 🔴 save.gd 补 `chapter_chest_claimed` 序列化（save 写 / load 读 / 钳制 bool / 默认 false）+ 旧档兼容回归（旧档无此字段 → 默认 false，不丢进度）。
2. 🟡 game.gd:510 `_cast_skill`：skill_id 兜底改 `skill.get("id", skill.get("name", &"skill"))`——敌方技能显示真实名。
3. 🟡 game.gd：`_apply_buff` 内补发 `status_changed`（所有应用路径）；`_tick_unit_timers` 补发移除（duration=0）。
B. v0.15 主线规则：
4. 主线不可选关：`start_battle(level)` 主线校验只接受"当前最高未通关关"；新增只读 `get_next_level() -> int`；`unlocked_level` 推进不变（通关 N 自动解锁 N+1）。
5. 主线不可扫荡：`sweep_level` 保留入口但主线禁用（仅秘境，阶段 3 实装）——主线不显示/不调用。
6. 首通掉落小钰碎片：`_resolve_victory` 首通时 `fragments["xiao_yu"] += 1`（普通关）/ `+= 3`（章节 BOSS 关，数值读 Data），发 `fragments_updated`；一次性不重复。
7. 主线不可重打：非首通重复胜利经验对主线不再适用（主线本就不能重打，确保无重复挑战路径）。
【验收】① 刷新后章节宝箱不重复领；② 敌方放技能显示真实技能名；③ 状态变化实时刷新；④ 主线只能打下一关（选关被拒）、无扫荡入口；⑤ 首通拿小钰碎片（普通 1/BOSS 3）、刷新后碎片还在；⑥ 旧档读入不报错。

> 【A 复核结论（v0.9·B）】三处修复与主线规则验收通过。小钰碎片常量裁决：**立即迁移至 Data**（契约 §3.3 数值归 Data）——特批本轮 data.gd 修改一次：data.gd 新增 `XIAOYU_FRAGMENT_FIRST_CLEAR := 1` / `XIAOYU_FRAGMENT_BOSS := 3`，game.gd 删除本地 `FIRST_CLEAR_XIAOYU_FRAGMENT*` 并改用 Data 常量（微补丁）。

---

## v0.10 变更单（战斗判定细则 + 星级体系 + 开局运营，契约 v0.10）

### B 变更单（scripts/data.gd / scripts/save.gd / scripts/game.gd）

【变更单】契约 v0.10：战斗判定细则 + 升星 1~10 星 + 开局资源（总指挥 A 派单）
【必读】docs/设计文档.md v0.18（§2.1 / §8.3 / §8.4 / 附录 B）、docs/契约.md（§1.2 / §3.5 / §3.6 / §3.8 / §3.9 / 变更记录 v0.10）、scripts/contract.gd（只读，v0.10 已声明 mech_star_updated）
【只允许修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md
1. data.gd 新增升星与边界常量：`MAX_STAR := 10`、`STAR_STAT_GAIN := 0.08`（每星基础属性 +8%）、`STAR_FRAGMENT_COST := {R:30, SR:60, SSR:100}`（片/星）、`BASE_LEVEL_CAP := 100`、`STAR_6_UNLOCK_LEVEL := 100`、`STAR_LEVEL_CAP_GAIN := 20`（6 星起每星 +20）、`SUMMON_TICKET_VALUE := 300`（1 券 = 300 钻 = 1 抽）。
2. save.gd：`mechs` 每机娘增加 `star`（1~10，默认 1）；旧档补齐（默认 1）。**开局资源仅新档**：默认档金币 1000 + 钻石 300（旧档保留原值，不覆盖）。
3. game.gd：
   - **战斗判定细则**（§8.3/v0.18）：先闪避后暴击（被闪避不判暴击）；护盾可叠加、优先吸收、不吃减伤；嘲讽只影响普攻+单体技；被控不加能量、不叠新控；追击/连击/反击算普攻（可触发被动/可暴击闪避/可获能量，追击不可再触发追击）；多波我方状态延续、敌波刷新；失败无惩罚可重试、不做复活、治疗溢出浪费；普攻目标兜底同列→最近→任意。
   - **升星**：新增 `upgrade_star(mech_id)`（校验碎片足够且满足解锁条件：1~5 星随时、6~10 星需等级 ≥ 100；扣碎片、star+1、发 mech_star_updated）、`star_cost(mech_id) -> Dictionary`（{fragments_needed, fragments_have, can_upgrade, unlock_reason}）、`get_level_cap(mech_id) -> int`（基础 100 + 星级>5 时每星 +20）；属性按星数 ×1.08^(star-1) 加成（_mech_stats 应用）。
   - **等级边界**：`upgrade()` 受 `get_level_cap` 限制（到上限不可升）；满级经验不累计（到上限经验不再增加）。
   - **召唤券定值**：`summon()` 支持 1 券 = 300 钻 = 1 抽（券可当 300 钻用；仍以钻石为主，券 >0 时优先扣券）。
   - 开局资源：仅新档（无存档时）gold=1000、diamond=300。
【验收】① 升星 1~5 星正常、6~10 星满级解锁生效、等级上限随星提升、属性 +8%/星；② 满级不可再升、经验不累计；③ 战斗判定细则全生效（先闪避后暴击/护盾叠加/嘲讽范围/被控不加能量/追击算普攻等）；④ 新档开局金币 1000+钻石 300、旧档不重置；⑤ 召唤券可当 300 钻抽卡；⑥ 刷新后星级/等级上限都在。
【汇报】末尾输出【交付说明】。

---

### C 变更单（机娘界面星级/升星显示）

【变更单】契约 v0.10：机娘界面星级与升星（总指挥 A 派单）
【必读】docs/契约.md（§3.5 / §3.6 / 变更记录 v0.10）、scripts/contract.gd（只读）
【只允许修改】scenes/Main.tscn、scripts/main_ui.gd、scenes/Formation.tscn、scripts/formation_ui.gd（或机娘详情页，若后续新增场景则列入）
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd
1. 机娘列表/详情显示**星级**（★1~★10）与**等级上限**（用 `Game.get_level_cap(id)` 只读）。
2. 新增**升星按钮**：显示所需碎片（`Game.star_cost(id)` 只读：需要/拥有/可否升/解锁原因），点按调 `Game.upgrade_star(id)`；connect `mech_star_updated` 刷新星级/属性。
3. 升星相关提示（碎片不足 / 满级解锁条件未达）以只读数据展示。
【验收】① 星级/等级上限正确显示；② 升星按钮可用、碎片不足或未满级时有提示；③ 升星后星级/属性刷新；④ 无数值赋值、无 emit。

> 【A 复核结论（v0.10·B/C）】升星体系/战斗判定/开局资源/召唤券定值验收通过。两处裁决：① `Game.star_cost()` 字段名以 B 实现为准 `{fragments, level_required}`（契约 §3.6 已对齐，免 B 返工）；② C 的"升级原因优先、无则升星原因"共用 message_label 方案接受；观察项续留（UI 按 Data 同公式换算星级属性，不阻塞）。
【汇报】末尾输出【交付说明】。
【汇报】末尾输出【交付说明】。

---

### C 变更单（scenes/Main.tscn / scripts/main_ui.gd / scenes/Battle.tscn / scripts/battle_view.gd）

【变更单】契约 v0.9：主线界面适配（总指挥 A 派单）
【必读】docs/契约.md（§1.3 / §3.6 / §3.9 / 变更记录 v0.9）
【只允许修改】scenes/Main.tscn、scripts/main_ui.gd、scenes/Battle.tscn、scripts/battle_view.gd
【只读】scripts/game.gd、scripts/save.gd、scripts/data.gd、scripts/contract.gd、scenes/Formation.tscn、scripts/formation_ui.gd
1. main_ui.gd / Main.tscn：主线改为**单一"挑战第 N 关"按钮**（用 `Game.get_next_level()` 显示当前关，去掉关卡选择列表）；无扫荡入口。
2. 首通奖励提示：通关后显示"首通奖励：金币+钻石+小钰碎片×N"（level_cleared / fragments_updated 信号）。
3. battle_view.gd：去掉主线扫荡按钮（扫荡仅秘境、阶段 3 再做）；2x 加速按钮保留。
【验收】① 主线只能显示并挑战下一关、无选关列表；② 首通提示含小钰碎片；③ 战斗界面无扫荡入口、2x 加速可用；④ 无数值赋值、无 emit。
【汇报】末尾输出【交付说明】。

---

## v0.11 变更单（战斗"前后排"轴修正，对齐设计文档 §1.5；**不升契约版本**——无新信号/新入口/新存档字段/新术语）

> 背景：设计文档 v0.17 §1.5 已定稿"每方 3x3，**靠近敌方一侧为前排**（我方最右列 / 敌方最左列为前排，先接敌）"；当前实现把"前排"做成了**上方行 row0**（敌方坦克排 row0、目标选择 `row*10` 行小优先），与设计不符，玩家实测反馈"掉血的是左侧/上方角色、位置感违和"（问题清单 #22）。A 裁决方案 A：按设计改轴，仅动 game.gd 逻辑。

### B 变更单（仅 scripts/game.gd）

【变更单】v0.11：战斗"前后排"轴修正——前排 = 靠近敌方一侧的列（总指挥 A 派单）
【必读】docs/设计文档.md（§1.5 九宫格方向与朝向、§2.5 技能目标 front/back、§8.3 目标选择）、docs/契约.md（§3.9 战斗 2.0）、docs/问题清单.md（#22）、scripts/game.gd（当前实现）
【只允许修改】scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md、scripts/data.gd、scripts/save.gd、scenes/*、其余 scripts/*

1. **`_spawn_wave`（敌方 AI 排阵）**：前排（tank 类肉盾）改排 **col 0**（靠近我方一侧），其余输出单位排 col 1/2；行 row 仅用于同列内铺开（保持每波 ≤5、位置无属性加成）。
2. **`_default_attack_target`（普攻目标）**：优先级 = 嘲讽 → 同列 → **靠近敌方一侧优先** → 列距兜底。把现 score 中的 `row * 10`（行小=前排）替换为"靠近敌方侧"列优先项：我方攻击者打敌方时敌方 col 越小越优先（前排 col0）；敌方攻击者打我方的列越大越优先（我方前排 col2）。"同列优先"（same_col*1000）权重保持最高。
3. **`_targets_for` 的 "front" / "back"（AOE 目标）**：front = 靠近敌方一侧的那一列（敌方视角 col0 / 我方视角 col2）；back = 远离侧（敌方 col2 / 我方 col0）。原 `_row_targets(enemies, row)` 按行取目标的逻辑替换为按列取（可新增列取目标辅助函数）。
4. **`_ensure_default_formation`（默认兜底阵型）**：默认布局改**我方右列（col2）优先**（建议 `[[0,2],[1,2],[2,2],[0,1],[1,1]]`，即右列 3 前排 + 中列 2 个），新档开局观感 = 我方前排靠右、面向敌方；旧档已有阵型不受影响。
5. **"追击同排"（`_next_target_same_row`）本轮不动**：保持"同行"语义；"同排"的最终语义（行 or 列）列为观察项，待策划官 P 澄清后另行处理，不阻塞本轮。

【约束】不改 scripts/data.gd（数值唯一来源，本轮只动逻辑）；不新增信号 / 入口 / 存档字段；不动契约版本（v0.11）；不 emit 新信号；UI 无需改动（格子位置由 row/col 决定，逻辑改后显示自动正确）。
【验收】① 战斗开始敌方坦克显示在敌方棋盘最左列（col0）、我方默认阵型小钰/阿岚显示在右列（col2）；② 目标选择：敌方 col0（前排）先被打，我方 col2（前排）先被打；③ front/back AOE 命中列正确；④ 逻辑推演第 1 章 5 关仍可正常战斗（不出现必胜/必败异常）；⑤ 无语法错误、契约红线零违反（不 emit、不改数值表、不写 UI）。
【汇报】末尾输出【交付说明】：逐条说明 5 处改动、推演验证结果、遗留说明（"追击同排"观察项）。

---

## v0.12 变更单（追击"同排"= 同一列；契约 v0.12 / 设计文档 v0.19）

> 背景：策划官 P 已正式澄清（问题清单 #21/#22 遗留）：① "被控不加能量"**含受击**（当前 game.gd 已符合，仅措辞确认，无代码改动）；② "追击同排"的"排" = **同一列**（col 相同，与设计文档 §1.5"前排=列"自洽）——追击目标需从"同行"改为"同列"。契约已升 v0.12，设计文档 P 修订中（v0.19）。

### B 变更单（仅 scripts/game.gd）

【变更单】契约 v0.12：追击目标"同排"改按"同一列"匹配（总指挥 A 派单）
【必读】docs/契约.md（§3.9 ⑤ 与变更记录 v0.12）、docs/设计文档.md（§2.5.1 小钰大招/绯小技2"追击同排（同一列）"；若 P 尚未完成 v0.19 修订，以本节文字为准）、docs/问题清单.md（#22 遗留）、scripts/game.gd（_next_target_same_row 与 _handle_kill 调用点）
【只允许修改】scripts/game.gd
【只读】scripts/contract.gd、docs/契约.md、docs/设计文档.md、scripts/data.gd、scripts/save.gd、其余 scripts/* 与 scenes/*
1. `_next_target_same_row(attacker, target)`（约 868~873 行）改为按**列**匹配：追击目标 = 与 target **同一 col（列相同）** 的下一个存活敌人（row 不限；跳过 target 自己；返回该侧数组里第一个满足者）。建议函数改名 `_next_target_same_col` 并同步 `_handle_kill`（约 858~860 行）的调用点与注释。
2. 无同列存活目标 → 返回空（不追击），行为与现状兜底一致。
【约束】只改 game.gd；不新增信号 / 入口 / 存档字段；契约版本 v0.12 已由 A 更新完毕（contract.gd 无本逻辑，无需动）；**#21（被控不加能量含受击）P 已澄清、当前实现已符合，本轮无需改动**（不要动 _gain_energy）。
【验收】① 静态推演：小钰大招击杀敌方 col0 目标后，追击敌方 col0 下一个存活者（存在时）；击杀 col1 目标则追击 col1 下一个；② 无同列目标不追击；③ 无语法错误、契约红线零违反（不 emit、不改数值表、不写 UI）。
【汇报】末尾输出【交付说明】。

> 【A 复核结论（v0.12）】① **P 设计文档 v0.19 验收通过**：8 处改动（头部/文末版本号 v0.19、v0.19 修订要点 2 条、§2.5.1 小钰/绯"追击同排（同一列）"、§2.5.2 影/冥追击同列明确、§8.3 控制条目补"（含受击：被控期间受击也不加能量）"），数值全部未动；② **B game.gd 追击按列验收通过**：`_next_target_same_col`（按 col 匹配、跳过 target 自己、row 不限、无同列返回空）+ `_handle_kill` 调用点同步，`_next_target_same_row` 代码零残留，Godot headless EXIT=0 日志零错误；③ **契约 v0.12 双载体同步**（§3.9 ④"被控不加能量（含受击）"、⑤ 追击同列，变更记录 v0.12）；④ **#21**（被控含受击）P 澄清后实现已符合，无代码改动；⑤ **部署**：Web 重导出 + SFTP 上传完成、在线版已生效（index.pck 426616B 验证通过），GitHub 已推送（9e5ca28）。

