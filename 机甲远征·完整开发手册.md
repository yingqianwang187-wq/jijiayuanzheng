# 机甲远征 · 完整开发手册（零基础 · Godot 4 · 机娘放置 · 多人 GitHub · 一册通）

> 本手册把"协作流程 / 游戏方案 / 实际操作 / GitHub 协作"融合成一份，并已纳入最新确认：**机甲少女（机娘）题材 + 轻剧情 + 系统"设计写全、代码分期"**。
> 你只干三件事：**贴提示词 → 存文件 → F5 运行**。不写一行代码。

## 目录

- 〇、先看全貌
- 一、8 条核心原则
- 二、游戏设定与 MVP 边界（机娘 + 轻剧情）
- 三、角色分工 + 文件所有权
- 四、策划会话 P 与《游戏设计文档》
- 五、契约文档
- 六、MVP 任务拆分
- 七、实际操作（12 步）
- 八、进阶机制（防越权/防失忆）
- 九、BUG 修复闭环
- 十、GitHub 多人协作
- 十一、卡住了怎么办 + 模板速查

---

## 〇、先看全貌

- **要做的游戏**：科幻**机甲少女（机娘）**放置挂机——你是指挥官，率机娘小队自动打怪，在线涨金币，升级机娘，打赢当前关解锁下一关，配一句轻剧情。
- **技术路线**：Godot 4；第一版导出网页挂网站，之后可打包手机 App。
- **协作方式**：策划 → 契约 → 多个 AI 会话分工干活；多名伙伴用 GitHub 同步。
- **核心心法**：**每个信息只有一个所有者；改别人东西 = 提"变更申请"（GitHub 里就是 Pull Request）。**

---

## 一、8 条核心原则（宪法，全程生效）

> 存成 `docs/工作原则.md`，每个新会话开场都要先读。

1. **先确认意图再动手**：动手前用自己的话复述需求，对"做什么/不做什么/做到什么程度"有 **≥90% 把握**才开工，不到就继续追问。口诀："先别动手，先问我，直到你有 90% 把握再开始。"
2. **记忆只认文件不认对话**：一切以后还要用的信息必须落盘成文件。判定："明天换一个新会话，只看文件能不能接着干？"
3. **一个文件只有一个所有者**：改别人文件 = 违规，只能提变更申请。
4. **先做能玩的最小版本，再慢慢加功能**。
5. **每一步必须能运行，否则不算交付**。
6. **AI 不会真的动你的电脑**：落盘、保存、运行、GitHub 提交都由"人"执行；AI 只负责想和写。
7. **拿不准就停，不硬猜**。
8. **永远能回到上一个能玩的版本**（备份 / Git Commit）。

---

## 二、游戏设定与 MVP 边界（机娘 + 轻剧情）

### 2.1 一句话定位

科幻机甲少女（机娘）题材的放置挂机推关游戏——你是指挥官，率机娘小队自动打怪，在线持续涨金币，用金币升级机娘，打赢当前关解锁下一关，每章配 1~2 句轻剧情。

### 2.2 意图确认（贴给会话 A 的两段）

```text
先别动手，也先别写代码。请你反复向我提问，直到你对我想要的东西有至少 90% 的把握。
确认前请先复述一遍：这个游戏要做什么、不做什么、做到什么程度算完成。
我确认无误后，你才能开始工作。
```

```text
我想做一个【机甲少女（机娘）放置挂机】游戏（暂定名：机甲远征）。
- 引擎：用 Godot 4 做，第一版先导出成网页挂到我网站上，以后也能打包成手机 App。
- 题材画风：科幻机甲少女（机娘）；先用简单头像/剪影代替立绘，后续再换正式立绘。
- 剧情：轻剧情——一句话世界观 + 每章解锁时 1~2 句台词，不做长剧情。
- 核心玩法：放置挂机 + 推关。机娘小队自动打怪，我只负责升级和点开下一关。
- 挂机：在线时每隔一段时间自动加金币（离线收益第一版可先不做）。
- 战斗：进入关卡后，我方机娘和敌人自动对打（按节拍结算伤害），有血条；赢了得金币并解锁下一关，输了可重试。
- 养成：用金币升级每台机娘（升级提高攻击和血量）。
- 长期会做（现在先只设计、以后分阶段做）：抽卡召唤、商城、背包、开箱等系统。
- 第一版规模：2 位机娘、5 个关卡、纯单机、进度存档。
- 我不懂编程，请用最朴素的话和我确认需求。
```

> 等 AI 复述，一致就回"确认无误"，不一致就纠正，直到一致。

### 2.3 MVP 边界（"刹车表"）

| 第一版做（阶段 0） | 第一版不做（先砍掉） |
|---|---|
| 2 位机娘（1 近战 + 1 远程） | 账号 / 登录 / 排行榜 / 服务器 |
| 5 个关卡，每关 1 波敌人 | 真人实时对战 |
| 自动战斗 + 血条显示 | 抽卡 / 十连（阶段 1 再做） |
| 在线挂机自动涨金币 | 商城 / 背包 / 装备 / 技能（阶段 2+ 再做） |
| 升级养成（等级 → 攻击/血量） | 复杂美术（先用简单图形，后续换立绘） |
| 本地存档（重开后进度还在） | 音效、多语言、长剧情 |
| 一句话世界观 + 每章 1 句台词 | 离线收益 |

---

## 三、角色分工 + 文件所有权

> 📌 **骨架先定、任务后拆**：这里的"几个会话 + 谁管哪些文件"是**稳定框架**，项目一开始就定型，不随设计文档改动；它由"用 Godot 做游戏"和技术架构基本确定。设计文档影响的是"**要做哪些系统的任务**"，那要等 **A 在 P 设计确认之后**才拆。团队可以**随阶段增员/细分**（游戏大了再加角色），但核心 P/A/B/C/D 一开始就定死。

### 3.1 五个会话

| 编号 | 角色 | 一句话职责 |
|---|---|---|
| P | 策划官 | 先写《游戏设计文档》——世界观/机娘/货币/抽卡/商城/背包等，**给你选项让你确认，不写代码** |
| A | 总指挥（兼契约官） | 拆任务、派单、验收、装配；唯一能改契约文档和 project.godot |
| B | 数值+玩法+存档 | 写 data.gd / save.gd / game.gd |
| C | 画面+界面 | 写 Main.tscn / Battle.tscn / main_ui.gd / battle_view.gd |
| D | 测试官（QA） | 只读验收、复现 BUG、写报告，不写任何代码 |

> 小团队里 **P 一般由你自己（总指挥）先开**，设计文档定稿后归你维护；人多时也可以分给一个"策划"伙伴。

### 3.2 项目文件结构 + 所有权

```text
机甲远征/                     ← 整个文件夹就是 Godot 项目
├── project.godot            ← A（项目配置：主场景、自动加载、分辨率）
├── .gitignore               ← A（GitHub 忽略规则）
├── icon.svg                 ← Godot 默认图标（可选）
├── docs/
│   ├── 工作原则.md          ← A（第一节的 8 条原则）
│   ├── 设计文档.md          ← P（策划官，先想全、你确认后才定稿）
│   ├── 契约.md              ← 契约官（=A）
│   └── 项目状态.md          ← A
├── scenes/
│   ├── Main.tscn            ← C（主界面场景）
│   └── Battle.tscn          ← C（战斗场景）
└── scripts/
	├── contract.gd          ← 契约官   常量/信号名/术语
	├── data.gd              ← B        机娘/敌人/关卡数值
	├── save.gd              ← B        存档读写
	├── game.gd              ← B        战斗 + 挂机 + 养成逻辑
	├── main_ui.gd           ← C        主界面脚本（挂在 Main.tscn）
	└── battle_view.gd       ← C        战斗画面脚本（挂在 Battle.tscn）
```

### 3.3 引擎映射（HTML 思维 → Godot 思维）

| HTML/JS 概念 | Godot 4 对应 |
|---|---|
| `js/xxx.js` 文件 | `.gd` 脚本 + `.tscn` 场景（都是文本） |
| 全局对象（Game/Save/Data/Contract） | **自动加载 Autoload**（单例） |
| 事件（emit/on） | **信号 Signal** |
| 双击 index.html 运行 | 导入项目 → 按 **F5** |
| 挂网站 | 导出 **Web** → 上传 → https 访问 |

---

## 四、策划会话 P 与《游戏设计文档》（先想全，再找你确认）

> 这是本轮新增的关键一步：**把"想全面一点再确认"落成一个固定角色和一个文件。**
> 流程：P 出设计文档 → 每个系统给你"2~3 个方案 + 讲影响" → 你选 → 定稿写进 `docs/设计文档.md` → 之后 A 据此更新契约、拆任务。
> ⏱ **时序提醒**：**A 总指挥的契约、拆任务、派单，都要等 P 设计文档被你确认之后才开始**——因为 B/C 要做哪些系统、契约里的玩法，全取决于设计文档定了什么，提前做会做错方向。P 出设计期间，A 可以先做**基建准备**（建文件夹、存 8 条原则、建 GitHub 仓库、搭 Godot 项目骨架），这些不依赖设计内容。

### 4.1 策划官提示词（贴给会话 P）

```text
你是【策划官】。为一个零编程基础的开发者，把"机甲少女（机娘）放置挂机游戏"做一份完整设计。
只输出设计文档，不写任何代码。请产出并写入 docs/设计文档.md，按下面结构：
① 世界观与轻剧情：一句话世界观 + 每章 1~2 句剧情/机娘台词（轻量，不做长剧情）。
② 机娘设定：初始 2 位机娘（名字、稀有度、定位、属性、成长、升级花费），后续可扩展。
③ 货币与经济循环：金币（软货币，阶段0就有）、钻石（抽卡货币，阶段1）；说清"产出途径"和"消耗途径"。
④ 抽卡/召唤系统：卡池规则、概率、保底、重复机娘怎么转化（设计全，代码阶段1做）。
⑤ 商城：金币商城/钻石商城（设计全，阶段3做）。
⑥ 背包：道具/材料列表与用途（设计全，阶段2做）。
⑦ box/开箱：作为奖励或抽卡变体（设计全，阶段3做）。
⑧ 数值框架：金币产出速度、升级消耗曲线、关卡难度曲线（给初版即可）。
⑨ 分期实现计划：阶段0~4 各做什么（阶段0 只做金币+升级+5关推关）。
要求：
- 每个系统都要"给我 2~3 个方案 + 讲清每个方案对玩家的影响"，让我确认后再定稿；我确认前不要定死。
- 用最朴素的话写，我不懂编程。
- 末尾写"版本 v0.1"。
```

> 💡 想要"事无巨细、逐项问明白"的**完整交接版**，直接整段粘贴《策划官·交接文件.md》给新会话 P 即可——它比上面这段更详细，含 **42 项必问清单**和追问规则。

### 4.2 设计文档长什么样（P 会按这个结构写）

```text
docs/设计文档.md
① 世界观与轻剧情
② 机娘设定（角色表）
③ 货币与经济循环
④ 抽卡/召唤
⑤ 商城
⑥ 背包
⑦ box/开箱
⑧ 数值框架
⑨ 分期实现计划
```

### 4.3 分期实现计划（设计全，代码逐步做）

| 阶段 | 做什么 | 状态 |
|---|---|---|
| 阶段 0（MVP） | 金币 + 升级 + 5 关推关 + 一句话世界观 | 现在做 |
| 阶段 1 | 抽卡/召唤（卡池、概率、保底） | 已设计，后做 |
| 阶段 2 | 商城 + 背包 | 已设计，后做 |
| 阶段 3 | box/开箱 + 每章剧情演出 + 装备/突破 | 已设计，后做 |
| 阶段 4 | 竞技场、账号 | 只记方向，另议 |

---

## 五、契约文档（贴给契约官）

```text
你是【契约官】。为一个零编程基础的开发者，用 Godot 4 固化"机娘放置挂机游戏"的公共契约。
只输出文档和常量脚本，不写业务逻辑。请产出并写入 docs/契约.md 和 scripts/contract.gd：
① 世界观与玩法：科幻机甲少女（机娘），指挥官率机娘小队自动推关，金币升级，本地存档（世界观细节以 docs/设计文档.md 为准）。
② 命名规范：自动加载单例用 Contract/Data/Save/Game；UI 脚本用 main_ui.gd / battle_view.gd。
③ 接口约定（重点）：
   - 金币/状态变化只由【Game】通过信号通知，UI 只连接信号显示、绝不自己改数值。
   - 存档只由【Save】负责，读失败返回默认值。
   - 机娘/敌人/关卡数值放 Data（scripts/data.gd）；常量/信号名/术语放 Contract（scripts/contract.gd）。
   - 战斗用"节拍结算"，UI 只刷新血条、不做物理碰撞。
④ 文件结构：列出每个文件属于谁（见第三节）。
⑤ 术语表：机娘=mech_girl；关卡=level；金币=gold；血条=HP bar；攻击=atk；血量=hp；升级=upgrade；信号=signal；自动加载=autoload。
末尾写"版本 v0.1"。
```

**project.godot 关键内容（总指挥生成时核对）**

```text
[application]
config/name="机甲远征"
run/main_scene="res://scenes/Main.tscn"

[autoload]
Contract="*res://scripts/contract.gd"
Data="*res://scripts/data.gd"
Save="*res://scripts/save.gd"
Game="*res://scripts/game.gd"

[display]
window/size/viewport_width=720
window/size/viewport_height=1280
window/stretch/mode="canvas_items"
window/stretch/aspect="keep"
```

> 版本号字段以你装的 Godot 版本为准，让 A 补齐；打开报错把报错贴回给 A 修。

---

## 六、MVP 任务拆分（给总指挥参考，按此派单）

| 序号 | 子任务 | 角色 | 输出文件 | 验收标准（≤3 条） |
|---|---|---|---|---|
| 1 | 数值表 | B | scripts/data.gd | ① 含 2 位机娘、5 关、每关敌人配置 ② 机娘字段：名/攻击/血量/成长/升级花费；关卡字段：敌人列表/奖励金币 ③ 只放数据不放逻辑 |
| 2 | 战斗+挂机+养成 | B | scripts/game.gd | ① 自动战斗：每 tick 双方结算伤害，敌人全灭→胜利解锁下一关，我方全灭→失败可重试 ② 挂机：按 Contract 间隔自动加金币并发信号 ③ 养成：upgrade(mechId) 扣金币、升等级、提高攻血 |
| 3 | 存档 | B | scripts/save.gd | ① load 返回 {gold, mechs, unlockedLevel} ② 读失败返回默认值不崩 ③ 用 FileAccess 读写 user://save.json，JSON 格式 |
| 4 | 战斗画面 | C | scenes/Battle.tscn + scripts/battle_view.gd | ① 画出双方机娘（简单图形）+ 血条 ② 血条随 Game 状态刷新 ③ 只读 Game 状态，绝不改数值 |
| 5 | 主界面 | C | scenes/Main.tscn + scripts/main_ui.gd | ① 显示金币/机娘列表/升级按钮/关卡选择 ② 按钮只调用 Game/Save，不自己改数值 ③ 有"进入战斗"按钮 |
| 6 | 装配 | A | project.godot | ① 配置 4 个自动加载 ② 主场景=Main.tscn ③ F5 能跑、不重写模块逻辑 |

---

## 七、实际操作（12 步，一步步照着做）

### 第 0 步：准备（一次性）

- [ ] 装 Godot：godotengine.org/download 下载 **Godot 4.x Standard（Windows）**，解压双击 `Godot_v4.x-stable_win64.exe`
- [ ] 桌面新建 `机甲远征`，里面建三个子文件夹：`docs`、`scenes`、`scripts`
- [ ] 打开"显示文件扩展名"（文件资源管理器 → 查看 → 勾选「文件扩展名」）
- [ ] 记事本/VS Code 编码设 **UTF-8**
- [ ] 开 AI 工具，建 5 个对话窗口：**P / A / B / C / D**（先只开 P 和 A）

### 第 1 步：存 8 条原则

- [ ] 把**第一节 8 条原则**存成 `机甲远征\docs\工作原则.md`

### 第 2 步：意图确认（会话 A）

- [ ] 贴**第二节 2.2 两段**（意图确认提示词 + 游戏想法），等 AI 复述，一致回"确认无误"

### 第 3 步：策划会话 P 出设计文档，找你确认（关键新增）

- [ ] 开会话 P，贴**第四节 4.1 的策划官提示词**
- [ ] P 会逐条给你"2~3 个方案 + 影响"，你选哪个就回哪个；**你确认前它不写代码、不定稿**
- [ ] 全部确认后，把它输出的《设计文档》存成 `机甲远征\docs\设计文档.md`

### 第 4 步：立契约（会话 A）

- [ ] 贴**第五节的契约官提示词**，拿输出
- [ ] 文档部分存 `docs\契约.md`；代码部分存 `scripts\contract.gd`
- [ ] 契约全文**再复制一份**留好（第 6、7 步给 B/C 开场用）

### 第 5 步：项目状态 + 拆任务（会话 A）

- [ ] 贴下面建状态文件，存 `docs\项目状态.md`：

```text
你是【总指挥】。请按下面格式输出一份 docs/项目状态.md（先用 v0.1，初始状态为空）。
【项目状态】
- 项目：机娘放置挂机游戏（机甲远征）
- 当前版本：v0.1
- 一句话现状：（填当前进度）
- 已完成（打勾）：（空）
- 进行中：（空）
- 下一步：（填第一步要做的事）
- 已知问题：无
- 最近一次备份：无
```

- [ ] 贴**拆任务提示词**，对照第六节存好任务清单：

```text
你是【总指挥】。请把"机娘放置挂机游戏"拆成可独立完成的子任务清单。
每个子任务写清：负责角色、输入文件、输出文件（只能写这一个）、验收标准（≤3条，可检查）。
先不要写代码，只输出任务清单。
```

### 第 6 步：开会话 B，写"数值 + 玩法 + 存档"

- [ ] 新建会话 B，第一句贴**开场白**：

```text
你是【游戏逻辑会话】。请先读下面两份材料，读完回复"已读，请下达任务"，不要动手。

【材料0：工作原则】docs/工作原则.md 的内容如下：
---（粘贴第 1 步那段 8 条原则全文）---

【材料1：公共契约】docs/契约.md 的内容如下：
---（粘贴第 4 步拿到的契约全文）---
```

- [ ] 等它回"已读"，再贴**派单**：

```text
【本轮任务】写 scripts/data.gd（机娘/敌人/关卡数值表）、scripts/save.gd（存档）、scripts/game.gd（自动战斗 + 挂机金币 + 升级养成）。
【必读】docs/契约.md、scripts/contract.gd
【只允许创建/修改】scripts/data.gd、scripts/save.gd、scripts/game.gd
【只读】scripts/contract.gd、scenes/、scripts/main_ui.gd、scripts/battle_view.gd
【规则】金币和状态变化只能通过 Game 的信号通知外界；存档只由 save.gd 的 Save 单例负责（用 FileAccess 读写 user://save.json）；数值只放 data.gd，不在代码里硬编码。
【汇报】末尾输出【交付说明】：新增对象/函数、签名、依赖的常量、改动的文件。
```

- [ ] 保存为 `scripts\data.gd`、`scripts\save.gd`、`scripts\game.gd`（文件名一字不差）

### 第 7 步：开会话 C，写"画面 + 界面"

- [ ] 新建会话 C，贴开场白（材料0 + 材料1 契约全文），等它回"已读"，再贴**派单**：

```text
【本轮任务】写 scenes/Main.tscn + scripts/main_ui.gd（主界面：金币/机娘列表/升级按钮/关卡选择）和 scenes/Battle.tscn + scripts/battle_view.gd（战斗画面：机娘和血条、开始/重试/返回按钮）。
【必读】docs/契约.md、scripts/contract.gd
【只允许创建/修改】scenes/Main.tscn、scenes/Battle.tscn、scripts/main_ui.gd、scripts/battle_view.gd
【只读】scripts/data.gd、scripts/save.gd、scripts/game.gd、scripts/contract.gd
【规则】只连接 Game 的信号来显示，绝不自己改金币、等级、存档。
【汇报】末尾输出【交付说明】：新增对象/函数、签名、依赖的常量、改动的文件。
```

- [ ] 保存为 `scenes\Main.tscn`、`scenes\Battle.tscn`、`scripts\main_ui.gd`、`scripts\battle_view.gd`

### 第 8 步：回会话 A，生成 project.godot 装配

- [ ] 贴**整合提示词**：

```text
你是【总指挥】。现在装配成可运行游戏。
【只允许修改】project.godot。
在 project.godot 里：把主场景设为 res://scenes/Main.tscn；把 Contract、Data、Save、Game 设为"自动加载(Autoload)"单例；设一个手机竖屏分辨率（如 720x1280）。
不重写任何模块逻辑；接口对不上就输出【接口冲突清单】退回。
```

- [ ] 保存为 `机甲远征\project.godot`

### 第 9 步：打开项目运行 + 会话 D 验收

- [ ] 打开 Godot → 项目管理器点「**导入(Import)**」→ 选中 `机甲远征\project.godot` → 打开
- [ ] 按 **F5** 试玩；报错/黑屏 → 看底部「**输出**」面板红字，复制回会话 A 排查
- [ ] 新建会话 D，贴开场白（材料0 原则 + 材料1 契约 + 材料2 所有权清单），等它回"已读"，再贴：

```text
你是【测试官QA】。请只读地验收整个游戏（对照契约和所有权清单），
从玩家视角检查：能选关卡进战斗、机娘和敌人自动对打、血条正常、赢了解锁下一关、金币和升级生效、重开后进度还在。
输出【验收结果】和【问题清单】。
```

### 第 10 步：修 BUG

- [ ] 有问题就按**第九节 BUG 闭环**走一遍

### 第 11 步：备份 + GitHub + 导出网页

- [ ] 每完成一个能玩的里程碑，复制整个 `机甲远征` 文件夹改名备份（或用 Git Commit，见第十节）
- [ ] 全部跑通后：菜单「**项目 → 导出**」→「**添加 → Web**」→ 首次会提示下载导出模板 → 设导出路径 → 「导出项目」，得到 `index.html` 等一组文件
- [ ] 上传到网站某目录，用 **https** 访问 `index.html`（Godot 网页版**不能用 file:// 双击本地打开**）

---

## 八、进阶机制（防越权 / 防失忆）

### 8.1 防越权规则语句（每个派单都带）

```text
你只被允许创建和修改文件：【（填你的文件）】。
除此之外的任何文件对你都是【只读】。
你可以在回答中引用它们的内容，但绝不能建议、写出或修改它们的代码。
如果你认为必须改别的文件，不要动手，输出【变更申请】。
```

### 8.2 变更申请格式

```text
【变更申请】
- 申请改的文件：
- 该文件的当前所有者：
- 为什么要改：
- 建议改成什么：
- 会影响哪些其他模块：
```

### 8.3 什么时候该换会话？

出现任意一条就该交接：AI 重复问"早就说过"的事；忘了自己刚写的函数名；答非所问、明显变啰嗦；你感觉"它失忆了"。

### 8.4 交接卡（每个会话的"记忆备份"）

```text
【交接卡】
- 项目：机娘放置挂机游戏
- 本会话角色：（你的角色）
- 项目现状一句话：（填）
- 本会话已做完的事：1) 2) 3)
- 本会话的关键决策：（例如：金币变化统一走 Game 信号，UI 只显示）
- 当前还没做完的事：（填）
- 我负责的文件：（填）
- 我依赖的约定：见 docs/契约.md、docs/设计文档.md
- 给下一位的提醒：（填）
```

### 8.5 命名约定

```text
Contract  → 常量、信号名、术语（scripts/contract.gd）
Data      → 机娘/敌人/关卡数值（scripts/data.gd）
Save      → 存档读写（scripts/save.gd）
Game      → 战斗/挂机/养成逻辑（scripts/game.gd）
main_ui.gd / battle_view.gd → 界面脚本
```

---

## 九、BUG 修复闭环（四件套）

**① 报 BUG：**

```text
【BUG 报告】
- 现象：（描述错在哪）
- 复现步骤：开始游戏 → ……
- 期望结果：……
- 实际结果：……
- 我怀疑涉及的模块：……
```

**② 总指挥只读排查：**

```text
你是【总指挥】。收到以下 BUG 报告（粘贴报告全文）。
请只读地查看相关脚本/场景文件，找出原因。
输出【定位结论】（谁错了、错在哪）和【指派决定】（让谁改、改什么）。不要自己改代码。
```

**③ 指派修复（带边界）：**

```text
你是【（角色名）】。经排查，问题在（某文件）。
现在只允许修改（该文件）：（说明改什么、改成什么）。
改完按【交付说明】汇报。
```

**④ 测试官回归：**

```text
你是【测试官QA】。BUG 已修复，请按原复现步骤重新验证，输出【回归结果】：已修复 / 未修复 + 现象。同时检查有没有引入新问题。
```

---

## 十、GitHub 多人协作

### 10.1 5 个必懂词

| 词 | 大白话 |
|---|---|
| 仓库 Repository | 项目文件夹的"云端版" |
| 提交 Commit | 打一个存档点，写一句"我改了什么" |
| 推送 Push | 把本地改动上传到云端 |
| 拉取 Pull | 把别人的改动下载到自己电脑 |
| 分支/合并 Pull Request(PR) | 各自在"草稿副本"改，改完开 PR，验收后合并 |

> GitHub 操作（提交/推送/拉取/合并）全部由"人"点鼠标，AI 不参与。

### 10.2 一次性搭建

**总指挥（你）：**
- [ ] 注册 GitHub，装 **GitHub Desktop**（desktop.github.com，全程不用命令行）
- [ ] Desktop → File → New repository → 名字 `机甲远征` → 本地路径选 `机甲远征` 文件夹 → Create
- [ ] 点 **Publish repository**，勾选 **Private（私有）**
- [ ] 根目录新建 `.gitignore`：

```text
# Godot 缓存（自动生成，不要传）
.godot/

# 导出产物（随时能再导出，不要传）
export/
build/
```

- [ ] github.com 打开仓库 → Settings → Collaborators → Add people → 邀请对方

**协作伙伴：** 注册账号 + 装 Desktop → 接受邀请 → File → **Clone repository** → 克隆到本机。

### 10.3 日常循环（每人每天 4 步）

1. **拉取**：Desktop 点 Pull origin / Fetch origin
2. **改自己的文件**：用 AI 会话干活，存进自己负责的文件
3. **提交**：勾选改动文件 → Summary 写一句"改了什么" → Commit
4. **推送/合并**：Push origin；大改动开 **Pull Request** 让总指挥验收后合并

### 10.4 多人分工表

| 人 | 角色 | 拥有的文件 | 用的 AI 会话 |
|---|---|---|---|
| 你 | 总指挥兼契约官 | project.godot、contract.gd、docs/*、.gitignore | P、A |
| 伙伴1 | 玩法+存档 | data.gd、save.gd、game.gd | B |
| 伙伴2 | 画面+界面 | Main.tscn、Battle.tscn、main_ui.gd、battle_view.gd | C |
| 伙伴3（可选） | 测试 | 不拥有文件，只报 BUG | D |

| 旧概念 | GitHub 落地 |
|---|---|
| 变更申请 | 开 Pull Request |
| 验收/回归 | 合并 PR 前的检查 |
| 所有权清单 | 上表"拥有的文件"列 |
| 契约.md/设计文档.md/项目状态.md | 共享文件，只有总指挥/策划能改，别人改要开 PR |
| 手动备份 | 每次 Commit = 存档点 + 云端一份 |

### 10.5 防冲突铁律

1. 一个文件同一时间只有一个人改
2. 每次开工先 Pull，收工就 Push
3. 提交时写清楚"改了哪个文件、为什么"
4. main 分支只放"能玩的稳定版本"
5. 共享文件改动前先在群里说一声

**真冲突了**：Pull/Push 提示 conflict → Desktop 打开冲突文件 → 里面有 `<<<<<<<` 和 `>>>>>>>` 标记 → 保留要的那份、删掉标记行 → 保存 → Commit。

---

## 十一、卡住了怎么办（速查）

| 现象 | 你该做什么 |
|---|---|
| AI 上来就写代码、没问我 | 回："先别动手，先问我，直到你有 90% 把握再开始。" |
| AI 说"改好了"但我没看到文件 | 正常——AI 不写你电脑，你要把代码复制进文件并保存 |
| Godot 里 F5 报错/黑屏 | 看底部「输出」面板红字，复制给会话 A；常见：路径错、自动加载没配、场景路径写错 |
| 打开项目提示缺文件 | 检查文件名/文件夹是否一字不差（.gd/.tscn 后缀、scenes/scripts 目录名） |
| 中文乱码 | 记事本/VS Code 另存为时编码选 UTF-8 |
| 重开后进度/金币丢了 | 让 B 在 save.gd 读 user://save.json 失败时返回默认值；网页版存档在浏览器里，清缓存会丢 |
| 导出网页后打不开 | Godot 网页版不能用 file:// 双击本地打开，必须上传网站用 https 访问 |
| 某个会话忘了之前的事 | 按第 6 步开场白重开新会话，把 8 条原则 + 契约全文 + 交接卡再贴一遍 |
| 感觉越做越乱 | 回到最近的备份 / Git 回退，从上一个能玩的版本继续 |
| GitHub 提示 conflict | 见 10.5，保留要的版本、删标记、重新 Commit |

---

## 附：全部模板速查（一张表）

| 模板 | 用途 | 位置 |
|---|---|---|
| 8 条核心原则 | 宪法，永久生效 | 第一节 |
| 意图确认提示词 | 逼 AI 先问再干 | 2.2 |
| 游戏想法描述 | 说清游戏需求（机娘） | 2.2 |
| MVP 刹车表 | 防止越做越大 | 2.3 |
| 策划官提示词 | 先写设计文档再找你确认 | 4.1 |
| 设计文档骨架 + 分期表 | 系统想全、代码分期 | 4.2 / 4.3 |
| 契约官提示词 | 立契约 + contract.gd | 第五节 |
| 项目状态模板 | 整局进度唯一真相 | 第 5 步 |
| 拆任务提示词 | 拆子任务 | 第 5 步 |
| 新会话开场白 | 把记忆重新灌进去 | 第 6 步 |
| 派单提示词（B） | 数值/玩法/存档 | 第 6 步 |
| 派单提示词（C） | 画面/界面 | 第 7 步 |
| 整合提示词 | 装配 project.godot | 第 8 步 |
| 验收提示词 | 检查交付是否合格 | 第 9 步 |
| 防越权规则语句 | 派单必带一句 | 8.1 |
| 变更申请格式 | 想改别人文件时 | 8.2 |
| 交接卡 | 会话换人记忆备份 | 8.4 |
| BUG 四件套 | 报/查/改/回归 | 第九节 |
| GitHub 搭建 + 日常循环 | 多人协作 | 第十节 |
| .gitignore | 忽略缓存/导出产物 | 10.2 |
