# ==================================================================
# .gitignore —— Godot 4 项目忽略规则（所有者：总指挥 A）
# ==================================================================

# Godot 4 自动生成的缓存/导入目录（每个人本机不同，不入库）
.godot/

# 临时文件与备份
*.tmp
*.bak
*_备份*/

# 陈旧二进制文档（工作原则已转为 docs/工作原则.md，.doc 为历史遗留，不入库）
docs/工作原则.doc

# 导出产物（如需入库再取消注释）
# /build/
# /exports/
